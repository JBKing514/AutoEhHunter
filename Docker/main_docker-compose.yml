services:
  data-ui:
    image: autoeh-data:local
    build:
      context: ./main
      dockerfile: Dockerfile
    container_name: autoeh-data-ui
    command: ["data-ui"]
    ports:
      - "8501:8501"
    volumes:
      - ./runtime:/app/runtime
    restart: unless-stopped
